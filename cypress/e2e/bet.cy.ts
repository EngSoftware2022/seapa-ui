describe('Test bet', () => {
    it('should be create new bet', () => {
      cy.visit('https://example.cypress.io')
    })
  
    it('should alert in error authenticate in url', () => {
      cy.visit('https://example.cypress.io')
    })
  })