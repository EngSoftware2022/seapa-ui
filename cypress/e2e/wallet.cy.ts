describe('Test wallet', () => {
    it('should list all transactions ', () => {
      cy.visit('https://example.cypress.io')
    })
  
    it('should be a must make a deposit', () => {
      cy.visit('https://example.cypress.io')
    })

    it('should be a must make a  withdrawal', () => {
        cy.visit('https://example.cypress.io')
    })

    it('sould be a list the last transactions', () => {
        cy.visit('https://example.cypress.io')
    })

    it('should show the balance', () => {
        cy.visit('https://example.cypress.io')
    })
    
  })