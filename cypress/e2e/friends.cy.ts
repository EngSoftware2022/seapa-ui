describe('Test friends', () => {
    it('should show friend requests', () => {
        cy.visit('https://example.cypress.io')
    })

    it('should make a friend request', () => {
        cy.visit('https://example.cypress.io')
    })

    it('should accept a friend request', () => {
        cy.visit('https://example.cypress.io')
    })

    it('should decline a friend request', () => {
        cy.visit('https://example.cypress.io')
    })

    it('should list friends', () => {
        cy.visit('https://example.cypress.io')
    })

    it('should delete friendships', () => {
        cy.visit('https://example.cypress.io')
    })
})